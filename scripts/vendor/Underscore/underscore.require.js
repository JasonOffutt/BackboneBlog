// Require.js module wrapper for Underscore. In a previous version, support
// for Common.js was removed from Underscore, so we need to wrap it in a 
// module definition to work.
define(['vendor/Underscore/underscore-1.3.3.min'], function() {
	return _;
});